import { CardOverDirective } from './card-over.directive';

describe('CardOverDirective', () => {
  it('should create an instance', () => {
    const directive = new CardOverDirective();
    expect(directive).toBeTruthy();
  });
});
