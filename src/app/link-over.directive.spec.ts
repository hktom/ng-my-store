import { LinkOverDirective } from './link-over.directive';

describe('LinkOverDirective', () => {
  it('should create an instance', () => {
    const directive = new LinkOverDirective();
    expect(directive).toBeTruthy();
  });
});
